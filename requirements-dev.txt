# =============================================================================
# requirements-dev.txt — Test dependencies (not needed in production)
# =============================================================================
# Install: pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.5
pytest-asyncio==0.24.0
# 0.7.9 is the newest release and still predates aiohttp 3.14's required
# `stream_writer` argument -- see tests/conftest.py for the shim that
# bridges it, and for the guard that removes itself once a fixed release
# exists.
aioresponses==0.7.9
